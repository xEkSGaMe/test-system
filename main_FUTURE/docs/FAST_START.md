# 🚀 Test System - Многокомпонентная система

## 📋 Описание
Тестовая система с микросервисной архитектурой:
- **Web Client** (Node.js) - веб-интерфейс
- **Auth Service** (Go) - аутентификация и авторизация  
- **Core Service** (C++) - основная бизнес-логика
- **Telegram Bot** (Python) - Telegram интеграция
- **PostgreSQL** - основная база данных
- **MongoDB** - документное хранилище
- **Redis** - кэш и сессии

## 🛠️ Быстрый старт

### Предварительные требования
- Docker 20.10+
- Docker Compose 2.0+
- Git

### Установка
```bash
# 1. Клонируйте репозиторий
git clone https://github.com/your-username/test-system.git
cd test-system

# 2. Настройте окружение
cp .env.example .env
# Отредактируйте .env при необходимости

# 3. Запустите систему
./scripts/start-dev.sh

# 4. Проверьте работу
./scripts/check-services.sh
```

## 🌐 Доступные сервисы
После запуска будут доступны:
- Web Client: http://localhost:3000
- Auth Service: http://localhost:8081
- Core Service: http://localhost:8082  
- API документация: http://localhost:8081/docs

Базы данных:
- PostgreSQL: localhost:5432
- MongoDB: localhost:27017
- Redis: localhost:6379

## 🧪 Разработка

### Работа с конкретным сервисом
```bash
# Запуск только auth-service с hot reload
cd auth
docker-compose -f ../docker-compose.dev.yml up auth-service

# Разработка web-client
cd web-client
npm install
npm run dev
```

### Добавление нового функционала
1. Создайте ветку от `develop`
2. Реализуйте изменения
3. Напишите/обновите тесты
4. Создайте Pull Request

## 📁 Структура проекта
```
test-system/
├── auth/          # Go сервис аутентификации
├── core/          # C++ основной сервис  
├── web-client/    # Frontend на React/Vue
├── telegram-bot/  # Python Telegram бот
├── docs/          # Документация
└── scripts/       # Вспомогательные скрипты
```

## 🔧 Скрипты
- `./scripts/start-dev.sh` - запуск для разработки
- `./scripts/start-prod.sh` - запуск для тестирования
- `./scripts/stop-all.sh` - остановка всех сервисов
- `./scripts/check-services.sh` - проверка доступности
- `./scripts/init-databases.sh` - инициализация баз данных

## 🐛 Отладка
```bash
# Просмотр логов
docker-compose logs -f [service_name]

# Вход в контейнер
docker exec -it [container_name] /bin/sh

# Пересборка сервиса
docker-compose build [service_name]
```

## 📚 Документация
- [Архитектура системы](./docs/ARCHITECTURE.md)
- [API спецификация](./docs/API.md)
- [Руководство по разработке](./docs/DEVELOPMENT.md)

## 👥 Вклад в проект
1. Форкните репозиторий
2. Создайте ветку для вашего изменения
3. Внесите изменения с тестами
4. Создайте Pull Request

## 📄 Лицензия
MIT